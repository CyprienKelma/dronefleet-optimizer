// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: dronefleet/v1/drone.proto

/* eslint-disable */
import { Timestamp } from "../../google/protobuf/timestamp";
import { DroneStatus, droneStatusFromJSON, droneStatusToJSON, Position } from "./common";

export const protobufPackage = "dronefleet.v1";

export interface Drone {
  id: string;
  position: Position | undefined;
  battery_percentage: number;
  speed_kmh: number;
  status: DroneStatus;
  current_mission_id: string;
  last_update: Date | undefined;
  solving_session_id: string;
  home_depot_id: string;
  battery_capacity_mah: number;
  consumption_per_km: number;
  max_flight_time_minutes: number;
  short_name: string;
}

export interface DroneTelemetry {
  drone_id: string;
  timestamp: Date | undefined;
  position: Position | undefined;
  battery_percentage: number;
  speed_kmh: number;
  status: DroneStatus;
  current_mission_id: string;
}

function createBaseDrone(): Drone {
  return {
    id: "",
    position: undefined,
    battery_percentage: 0,
    speed_kmh: 0,
    status: 0,
    current_mission_id: "",
    last_update: undefined,
    solving_session_id: "",
    home_depot_id: "",
    battery_capacity_mah: 0,
    consumption_per_km: 0,
    max_flight_time_minutes: 0,
    short_name: "",
  };
}

export const Drone: MessageFns<Drone> = {
  fromJSON(object: any): Drone {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      position: isSet(object.position) ? Position.fromJSON(object.position) : undefined,
      battery_percentage: isSet(object.batteryPercentage)
        ? globalThis.Number(object.batteryPercentage)
        : isSet(object.battery_percentage)
        ? globalThis.Number(object.battery_percentage)
        : 0,
      speed_kmh: isSet(object.speedKmh)
        ? globalThis.Number(object.speedKmh)
        : isSet(object.speed_kmh)
        ? globalThis.Number(object.speed_kmh)
        : 0,
      status: isSet(object.status) ? droneStatusFromJSON(object.status) : 0,
      current_mission_id: isSet(object.currentMissionId)
        ? globalThis.String(object.currentMissionId)
        : isSet(object.current_mission_id)
        ? globalThis.String(object.current_mission_id)
        : "",
      last_update: isSet(object.lastUpdate)
        ? fromJsonTimestamp(object.lastUpdate)
        : isSet(object.last_update)
        ? fromJsonTimestamp(object.last_update)
        : undefined,
      solving_session_id: isSet(object.solvingSessionId)
        ? globalThis.String(object.solvingSessionId)
        : isSet(object.solving_session_id)
        ? globalThis.String(object.solving_session_id)
        : "",
      home_depot_id: isSet(object.homeDepotId)
        ? globalThis.String(object.homeDepotId)
        : isSet(object.home_depot_id)
        ? globalThis.String(object.home_depot_id)
        : "",
      battery_capacity_mah: isSet(object.batteryCapacityMah)
        ? globalThis.Number(object.batteryCapacityMah)
        : isSet(object.battery_capacity_mah)
        ? globalThis.Number(object.battery_capacity_mah)
        : 0,
      consumption_per_km: isSet(object.consumptionPerKm)
        ? globalThis.Number(object.consumptionPerKm)
        : isSet(object.consumption_per_km)
        ? globalThis.Number(object.consumption_per_km)
        : 0,
      max_flight_time_minutes: isSet(object.maxFlightTimeMinutes)
        ? globalThis.Number(object.maxFlightTimeMinutes)
        : isSet(object.max_flight_time_minutes)
        ? globalThis.Number(object.max_flight_time_minutes)
        : 0,
      short_name: isSet(object.shortName)
        ? globalThis.String(object.shortName)
        : isSet(object.short_name)
        ? globalThis.String(object.short_name)
        : "",
    };
  },

  toJSON(message: Drone): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.position !== undefined) {
      obj.position = Position.toJSON(message.position);
    }
    if (message.battery_percentage !== 0) {
      obj.batteryPercentage = message.battery_percentage;
    }
    if (message.speed_kmh !== 0) {
      obj.speedKmh = message.speed_kmh;
    }
    if (message.status !== 0) {
      obj.status = droneStatusToJSON(message.status);
    }
    if (message.current_mission_id !== "") {
      obj.currentMissionId = message.current_mission_id;
    }
    if (message.last_update !== undefined) {
      obj.lastUpdate = message.last_update.toISOString();
    }
    if (message.solving_session_id !== "") {
      obj.solvingSessionId = message.solving_session_id;
    }
    if (message.home_depot_id !== "") {
      obj.homeDepotId = message.home_depot_id;
    }
    if (message.battery_capacity_mah !== 0) {
      obj.batteryCapacityMah = Math.round(message.battery_capacity_mah);
    }
    if (message.consumption_per_km !== 0) {
      obj.consumptionPerKm = message.consumption_per_km;
    }
    if (message.max_flight_time_minutes !== 0) {
      obj.maxFlightTimeMinutes = Math.round(message.max_flight_time_minutes);
    }
    if (message.short_name !== "") {
      obj.shortName = message.short_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Drone>, I>>(base?: I): Drone {
    return Drone.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Drone>, I>>(object: I): Drone {
    const message = createBaseDrone();
    message.id = object.id ?? "";
    message.position = (object.position !== undefined && object.position !== null)
      ? Position.fromPartial(object.position)
      : undefined;
    message.battery_percentage = object.battery_percentage ?? 0;
    message.speed_kmh = object.speed_kmh ?? 0;
    message.status = object.status ?? 0;
    message.current_mission_id = object.current_mission_id ?? "";
    message.last_update = object.last_update ?? undefined;
    message.solving_session_id = object.solving_session_id ?? "";
    message.home_depot_id = object.home_depot_id ?? "";
    message.battery_capacity_mah = object.battery_capacity_mah ?? 0;
    message.consumption_per_km = object.consumption_per_km ?? 0;
    message.max_flight_time_minutes = object.max_flight_time_minutes ?? 0;
    message.short_name = object.short_name ?? "";
    return message;
  },
};

function createBaseDroneTelemetry(): DroneTelemetry {
  return {
    drone_id: "",
    timestamp: undefined,
    position: undefined,
    battery_percentage: 0,
    speed_kmh: 0,
    status: 0,
    current_mission_id: "",
  };
}

export const DroneTelemetry: MessageFns<DroneTelemetry> = {
  fromJSON(object: any): DroneTelemetry {
    return {
      drone_id: isSet(object.droneId)
        ? globalThis.String(object.droneId)
        : isSet(object.drone_id)
        ? globalThis.String(object.drone_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      position: isSet(object.position) ? Position.fromJSON(object.position) : undefined,
      battery_percentage: isSet(object.batteryPercentage)
        ? globalThis.Number(object.batteryPercentage)
        : isSet(object.battery_percentage)
        ? globalThis.Number(object.battery_percentage)
        : 0,
      speed_kmh: isSet(object.speedKmh)
        ? globalThis.Number(object.speedKmh)
        : isSet(object.speed_kmh)
        ? globalThis.Number(object.speed_kmh)
        : 0,
      status: isSet(object.status) ? droneStatusFromJSON(object.status) : 0,
      current_mission_id: isSet(object.currentMissionId)
        ? globalThis.String(object.currentMissionId)
        : isSet(object.current_mission_id)
        ? globalThis.String(object.current_mission_id)
        : "",
    };
  },

  toJSON(message: DroneTelemetry): unknown {
    const obj: any = {};
    if (message.drone_id !== "") {
      obj.droneId = message.drone_id;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.position !== undefined) {
      obj.position = Position.toJSON(message.position);
    }
    if (message.battery_percentage !== 0) {
      obj.batteryPercentage = message.battery_percentage;
    }
    if (message.speed_kmh !== 0) {
      obj.speedKmh = message.speed_kmh;
    }
    if (message.status !== 0) {
      obj.status = droneStatusToJSON(message.status);
    }
    if (message.current_mission_id !== "") {
      obj.currentMissionId = message.current_mission_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DroneTelemetry>, I>>(base?: I): DroneTelemetry {
    return DroneTelemetry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DroneTelemetry>, I>>(object: I): DroneTelemetry {
    const message = createBaseDroneTelemetry();
    message.drone_id = object.drone_id ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.position = (object.position !== undefined && object.position !== null)
      ? Position.fromPartial(object.position)
      : undefined;
    message.battery_percentage = object.battery_percentage ?? 0;
    message.speed_kmh = object.speed_kmh ?? 0;
    message.status = object.status ?? 0;
    message.current_mission_id = object.current_mission_id ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
