// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: dronefleet/v1/depot.proto

/* eslint-disable */
import { Position } from "./common";

export const protobufPackage = "dronefleet.v1";

export interface Depot {
  id: string;
  name: string;
  position: Position | undefined;
  capacity: number;
  charging_slots: number;
}

function createBaseDepot(): Depot {
  return { id: "", name: "", position: undefined, capacity: 0, charging_slots: 0 };
}

export const Depot: MessageFns<Depot> = {
  fromJSON(object: any): Depot {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      position: isSet(object.position) ? Position.fromJSON(object.position) : undefined,
      capacity: isSet(object.capacity) ? globalThis.Number(object.capacity) : 0,
      charging_slots: isSet(object.chargingSlots)
        ? globalThis.Number(object.chargingSlots)
        : isSet(object.charging_slots)
        ? globalThis.Number(object.charging_slots)
        : 0,
    };
  },

  toJSON(message: Depot): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.position !== undefined) {
      obj.position = Position.toJSON(message.position);
    }
    if (message.capacity !== 0) {
      obj.capacity = Math.round(message.capacity);
    }
    if (message.charging_slots !== 0) {
      obj.chargingSlots = Math.round(message.charging_slots);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Depot>, I>>(base?: I): Depot {
    return Depot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Depot>, I>>(object: I): Depot {
    const message = createBaseDepot();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.position = (object.position !== undefined && object.position !== null)
      ? Position.fromPartial(object.position)
      : undefined;
    message.capacity = object.capacity ?? 0;
    message.charging_slots = object.charging_slots ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
