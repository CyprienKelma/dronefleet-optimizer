// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: dronefleet/v1/snapshot.proto

/* eslint-disable */
import { Timestamp } from "../../google/protobuf/timestamp";
import { Depot } from "./depot";
import { Drone } from "./drone";
import { Order } from "./order";
import { Warehouse } from "./warehouse";

export const protobufPackage = "dronefleet.v1";

export interface OptimizationSnapshot {
  session_id: string;
  timestamp: Date | undefined;
  depot: Depot | undefined;
  drones: Drone[];
  orders: Order[];
  warehouses: Warehouse[];
}

function createBaseOptimizationSnapshot(): OptimizationSnapshot {
  return { session_id: "", timestamp: undefined, depot: undefined, drones: [], orders: [], warehouses: [] };
}

export const OptimizationSnapshot: MessageFns<OptimizationSnapshot> = {
  fromJSON(object: any): OptimizationSnapshot {
    return {
      session_id: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      depot: isSet(object.depot) ? Depot.fromJSON(object.depot) : undefined,
      drones: globalThis.Array.isArray(object?.drones)
        ? object.drones.map((e: any) => Drone.fromJSON(e))
        : [],
      orders: globalThis.Array.isArray(object?.orders) ? object.orders.map((e: any) => Order.fromJSON(e)) : [],
      warehouses: globalThis.Array.isArray(object?.warehouses)
        ? object.warehouses.map((e: any) => Warehouse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: OptimizationSnapshot): unknown {
    const obj: any = {};
    if (message.session_id !== "") {
      obj.sessionId = message.session_id;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.depot !== undefined) {
      obj.depot = Depot.toJSON(message.depot);
    }
    if (message.drones?.length) {
      obj.drones = message.drones.map((e) => Drone.toJSON(e));
    }
    if (message.orders?.length) {
      obj.orders = message.orders.map((e) => Order.toJSON(e));
    }
    if (message.warehouses?.length) {
      obj.warehouses = message.warehouses.map((e) => Warehouse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OptimizationSnapshot>, I>>(base?: I): OptimizationSnapshot {
    return OptimizationSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OptimizationSnapshot>, I>>(object: I): OptimizationSnapshot {
    const message = createBaseOptimizationSnapshot();
    message.session_id = object.session_id ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.depot = (object.depot !== undefined && object.depot !== null) ? Depot.fromPartial(object.depot) : undefined;
    message.drones = object.drones?.map((e) => Drone.fromPartial(e)) || [];
    message.orders = object.orders?.map((e) => Order.fromPartial(e)) || [];
    message.warehouses = object.warehouses?.map((e) => Warehouse.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
