// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: dronefleet/v1/mission.proto

/* eslint-disable */
import { Timestamp } from "../../google/protobuf/timestamp";
import { Position, WaypointType, waypointTypeFromJSON, waypointTypeToJSON } from "./common";

export const protobufPackage = "dronefleet.v1";

export interface Waypoint {
  type: WaypointType;
  position: Position | undefined;
  related_order_id: string;
  related_warehouse_id: string;
}

export interface Mission {
  id: string;
  drone_id: string;
  order_ids: string[];
  route: Waypoint[];
  /** ACTIVE, COMPLETED, FAILED */
  status: string;
  start_time: Date | undefined;
  end_time: Date | undefined;
  estimated_battery_consumption: number;
  estimated_duration_minutes: number;
}

export interface MissionAssignment {
  drone_id: string;
  order_ids: string[];
  route: Waypoint[];
  estimated_battery_consumption: number;
  estimated_duration_minutes: number;
}

function createBaseWaypoint(): Waypoint {
  return { type: 0, position: undefined, related_order_id: "", related_warehouse_id: "" };
}

export const Waypoint: MessageFns<Waypoint> = {
  fromJSON(object: any): Waypoint {
    return {
      type: isSet(object.type) ? waypointTypeFromJSON(object.type) : 0,
      position: isSet(object.position) ? Position.fromJSON(object.position) : undefined,
      related_order_id: isSet(object.relatedOrderId)
        ? globalThis.String(object.relatedOrderId)
        : isSet(object.related_order_id)
        ? globalThis.String(object.related_order_id)
        : "",
      related_warehouse_id: isSet(object.relatedWarehouseId)
        ? globalThis.String(object.relatedWarehouseId)
        : isSet(object.related_warehouse_id)
        ? globalThis.String(object.related_warehouse_id)
        : "",
    };
  },

  toJSON(message: Waypoint): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = waypointTypeToJSON(message.type);
    }
    if (message.position !== undefined) {
      obj.position = Position.toJSON(message.position);
    }
    if (message.related_order_id !== "") {
      obj.relatedOrderId = message.related_order_id;
    }
    if (message.related_warehouse_id !== "") {
      obj.relatedWarehouseId = message.related_warehouse_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Waypoint>, I>>(base?: I): Waypoint {
    return Waypoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Waypoint>, I>>(object: I): Waypoint {
    const message = createBaseWaypoint();
    message.type = object.type ?? 0;
    message.position = (object.position !== undefined && object.position !== null)
      ? Position.fromPartial(object.position)
      : undefined;
    message.related_order_id = object.related_order_id ?? "";
    message.related_warehouse_id = object.related_warehouse_id ?? "";
    return message;
  },
};

function createBaseMission(): Mission {
  return {
    id: "",
    drone_id: "",
    order_ids: [],
    route: [],
    status: "",
    start_time: undefined,
    end_time: undefined,
    estimated_battery_consumption: 0,
    estimated_duration_minutes: 0,
  };
}

export const Mission: MessageFns<Mission> = {
  fromJSON(object: any): Mission {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      drone_id: isSet(object.droneId)
        ? globalThis.String(object.droneId)
        : isSet(object.drone_id)
        ? globalThis.String(object.drone_id)
        : "",
      order_ids: globalThis.Array.isArray(object?.orderIds)
        ? object.orderIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.order_ids)
        ? object.order_ids.map((e: any) => globalThis.String(e))
        : [],
      route: globalThis.Array.isArray(object?.route)
        ? object.route.map((e: any) => Waypoint.fromJSON(e))
        : [],
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      start_time: isSet(object.startTime)
        ? fromJsonTimestamp(object.startTime)
        : isSet(object.start_time)
        ? fromJsonTimestamp(object.start_time)
        : undefined,
      end_time: isSet(object.endTime)
        ? fromJsonTimestamp(object.endTime)
        : isSet(object.end_time)
        ? fromJsonTimestamp(object.end_time)
        : undefined,
      estimated_battery_consumption: isSet(object.estimatedBatteryConsumption)
        ? globalThis.Number(object.estimatedBatteryConsumption)
        : isSet(object.estimated_battery_consumption)
        ? globalThis.Number(object.estimated_battery_consumption)
        : 0,
      estimated_duration_minutes: isSet(object.estimatedDurationMinutes)
        ? globalThis.Number(object.estimatedDurationMinutes)
        : isSet(object.estimated_duration_minutes)
        ? globalThis.Number(object.estimated_duration_minutes)
        : 0,
    };
  },

  toJSON(message: Mission): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.drone_id !== "") {
      obj.droneId = message.drone_id;
    }
    if (message.order_ids?.length) {
      obj.orderIds = message.order_ids;
    }
    if (message.route?.length) {
      obj.route = message.route.map((e) => Waypoint.toJSON(e));
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.start_time !== undefined) {
      obj.startTime = message.start_time.toISOString();
    }
    if (message.end_time !== undefined) {
      obj.endTime = message.end_time.toISOString();
    }
    if (message.estimated_battery_consumption !== 0) {
      obj.estimatedBatteryConsumption = message.estimated_battery_consumption;
    }
    if (message.estimated_duration_minutes !== 0) {
      obj.estimatedDurationMinutes = message.estimated_duration_minutes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Mission>, I>>(base?: I): Mission {
    return Mission.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Mission>, I>>(object: I): Mission {
    const message = createBaseMission();
    message.id = object.id ?? "";
    message.drone_id = object.drone_id ?? "";
    message.order_ids = object.order_ids?.map((e) => e) || [];
    message.route = object.route?.map((e) => Waypoint.fromPartial(e)) || [];
    message.status = object.status ?? "";
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    message.estimated_battery_consumption = object.estimated_battery_consumption ?? 0;
    message.estimated_duration_minutes = object.estimated_duration_minutes ?? 0;
    return message;
  },
};

function createBaseMissionAssignment(): MissionAssignment {
  return { drone_id: "", order_ids: [], route: [], estimated_battery_consumption: 0, estimated_duration_minutes: 0 };
}

export const MissionAssignment: MessageFns<MissionAssignment> = {
  fromJSON(object: any): MissionAssignment {
    return {
      drone_id: isSet(object.droneId)
        ? globalThis.String(object.droneId)
        : isSet(object.drone_id)
        ? globalThis.String(object.drone_id)
        : "",
      order_ids: globalThis.Array.isArray(object?.orderIds)
        ? object.orderIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.order_ids)
        ? object.order_ids.map((e: any) => globalThis.String(e))
        : [],
      route: globalThis.Array.isArray(object?.route) ? object.route.map((e: any) => Waypoint.fromJSON(e)) : [],
      estimated_battery_consumption: isSet(object.estimatedBatteryConsumption)
        ? globalThis.Number(object.estimatedBatteryConsumption)
        : isSet(object.estimated_battery_consumption)
        ? globalThis.Number(object.estimated_battery_consumption)
        : 0,
      estimated_duration_minutes: isSet(object.estimatedDurationMinutes)
        ? globalThis.Number(object.estimatedDurationMinutes)
        : isSet(object.estimated_duration_minutes)
        ? globalThis.Number(object.estimated_duration_minutes)
        : 0,
    };
  },

  toJSON(message: MissionAssignment): unknown {
    const obj: any = {};
    if (message.drone_id !== "") {
      obj.droneId = message.drone_id;
    }
    if (message.order_ids?.length) {
      obj.orderIds = message.order_ids;
    }
    if (message.route?.length) {
      obj.route = message.route.map((e) => Waypoint.toJSON(e));
    }
    if (message.estimated_battery_consumption !== 0) {
      obj.estimatedBatteryConsumption = message.estimated_battery_consumption;
    }
    if (message.estimated_duration_minutes !== 0) {
      obj.estimatedDurationMinutes = message.estimated_duration_minutes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MissionAssignment>, I>>(base?: I): MissionAssignment {
    return MissionAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MissionAssignment>, I>>(object: I): MissionAssignment {
    const message = createBaseMissionAssignment();
    message.drone_id = object.drone_id ?? "";
    message.order_ids = object.order_ids?.map((e) => e) || [];
    message.route = object.route?.map((e) => Waypoint.fromPartial(e)) || [];
    message.estimated_battery_consumption = object.estimated_battery_consumption ?? 0;
    message.estimated_duration_minutes = object.estimated_duration_minutes ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
