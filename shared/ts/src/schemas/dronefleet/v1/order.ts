// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: dronefleet/v1/order.proto

/* eslint-disable */
import { Timestamp } from "../../google/protobuf/timestamp";
import {
  OrderPriority,
  orderPriorityFromJSON,
  orderPriorityToJSON,
  OrderStatus,
  orderStatusFromJSON,
  orderStatusToJSON,
  Position,
} from "./common";

export const protobufPackage = "dronefleet.v1";

export interface Order {
  id: string;
  pickup_location: Position | undefined;
  delivery_location: Position | undefined;
  status: OrderStatus;
  priority: OrderPriority;
  product_type: string;
  created_at: Date | undefined;
  assigned_drone_id: string;
  assigned_mission_id: string;
  solving_session_id: string;
}

function createBaseOrder(): Order {
  return {
    id: "",
    pickup_location: undefined,
    delivery_location: undefined,
    status: 0,
    priority: 0,
    product_type: "",
    created_at: undefined,
    assigned_drone_id: "",
    assigned_mission_id: "",
    solving_session_id: "",
  };
}

export const Order: MessageFns<Order> = {
  fromJSON(object: any): Order {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      pickup_location: isSet(object.pickupLocation)
        ? Position.fromJSON(object.pickupLocation)
        : isSet(object.pickup_location)
        ? Position.fromJSON(object.pickup_location)
        : undefined,
      delivery_location: isSet(object.deliveryLocation)
        ? Position.fromJSON(object.deliveryLocation)
        : isSet(object.delivery_location)
        ? Position.fromJSON(object.delivery_location)
        : undefined,
      status: isSet(object.status) ? orderStatusFromJSON(object.status) : 0,
      priority: isSet(object.priority) ? orderPriorityFromJSON(object.priority) : 0,
      product_type: isSet(object.productType)
        ? globalThis.String(object.productType)
        : isSet(object.product_type)
        ? globalThis.String(object.product_type)
        : "",
      created_at: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      assigned_drone_id: isSet(object.assignedDroneId)
        ? globalThis.String(object.assignedDroneId)
        : isSet(object.assigned_drone_id)
        ? globalThis.String(object.assigned_drone_id)
        : "",
      assigned_mission_id: isSet(object.assignedMissionId)
        ? globalThis.String(object.assignedMissionId)
        : isSet(object.assigned_mission_id)
        ? globalThis.String(object.assigned_mission_id)
        : "",
      solving_session_id: isSet(object.solvingSessionId)
        ? globalThis.String(object.solvingSessionId)
        : isSet(object.solving_session_id)
        ? globalThis.String(object.solving_session_id)
        : "",
    };
  },

  toJSON(message: Order): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.pickup_location !== undefined) {
      obj.pickupLocation = Position.toJSON(message.pickup_location);
    }
    if (message.delivery_location !== undefined) {
      obj.deliveryLocation = Position.toJSON(message.delivery_location);
    }
    if (message.status !== 0) {
      obj.status = orderStatusToJSON(message.status);
    }
    if (message.priority !== 0) {
      obj.priority = orderPriorityToJSON(message.priority);
    }
    if (message.product_type !== "") {
      obj.productType = message.product_type;
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at.toISOString();
    }
    if (message.assigned_drone_id !== "") {
      obj.assignedDroneId = message.assigned_drone_id;
    }
    if (message.assigned_mission_id !== "") {
      obj.assignedMissionId = message.assigned_mission_id;
    }
    if (message.solving_session_id !== "") {
      obj.solvingSessionId = message.solving_session_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Order>, I>>(base?: I): Order {
    return Order.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Order>, I>>(object: I): Order {
    const message = createBaseOrder();
    message.id = object.id ?? "";
    message.pickup_location = (object.pickup_location !== undefined && object.pickup_location !== null)
      ? Position.fromPartial(object.pickup_location)
      : undefined;
    message.delivery_location = (object.delivery_location !== undefined && object.delivery_location !== null)
      ? Position.fromPartial(object.delivery_location)
      : undefined;
    message.status = object.status ?? 0;
    message.priority = object.priority ?? 0;
    message.product_type = object.product_type ?? "";
    message.created_at = object.created_at ?? undefined;
    message.assigned_drone_id = object.assigned_drone_id ?? "";
    message.assigned_mission_id = object.assigned_mission_id ?? "";
    message.solving_session_id = object.solving_session_id ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
