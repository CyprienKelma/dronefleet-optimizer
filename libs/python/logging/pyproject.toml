[project]
name = "dronefleet-logging"
version = "0.1.0"
description = "Shared logging library for DroneFleet"
requires-python = ">=3.11"
dependencies = [
    "structlog>=24.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
